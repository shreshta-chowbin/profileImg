import React from 'react'
// import { useLocation } from 'react-router-dom'

function StuDetails() {

  // let locationObj=useLocation();
  return (
    <div>

      {/* <p>{locationObj && locationObj.state && locationObj.state.name ? locationObj.state.name : ""}</p>
      <p>{locationObj && locationObj.state && locationObj.state.email ? locationObj.state.email : ""}</p>
      <p>{locationObj && locationObj.state && locationObj.state.mobileNo ? locationObj.state.mobileNo : ""}</p>
      <p>{locationObj && locationObj.state && locationObj.state.profileImg ? locationObj.state.profileImg : ""}</p>
      <p>{locationObj && locationObj.state && locationObj.state.city ? locationObj.state.city : ""}</p>
      <p>{locationObj && locationObj.state && locationObj.state.state ? locationObj.state.state : ""}</p>
      <p>{locationObj && locationObj.state && locationObj.state.email ? locationObj.state.email : ""}</p> */}

    </div>
  )
}

export default StuDetails
